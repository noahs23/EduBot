Workflow:
Create a short-lived feature branch from main.
Commit small changes and merge them back into main via a pull request once complete and reviewed.
Rely heavily on automated testing and continuous integration (CI/CD) to ensure stability. 
